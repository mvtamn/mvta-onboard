import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.178",
  date: "2026-09-09",
  sections: [
    {
      heading: "Added",
      items: [
        "A standard's detail page says when a corrective action plan is owed. The rail described how the standard is measured, who owns it and what each band charges — but not the window that decides whether a CAP is required, which was configurable in Administration and visible nowhere else. It now reads as a sentence beside the bands: the band is the money, the window is when a plan is owed, and together they are what the contract does when the standard is missed. A standard with no window says so rather than showing the em dash an empty row uses.",
      ],
    },
  ],
};

export default entry;
