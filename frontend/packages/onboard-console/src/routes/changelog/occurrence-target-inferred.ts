import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.178",
  date: "2026-09-09",
  sections: [
    {
      heading: "Changed",
      items: [
        "An occurrence standard with no stated target is targeted at none. The scorecard, the standard's detail and the issued report read \"0 occurrences\" where they read \"No target set\", because a counted event with nothing stated is one the contract means not to happen. A Meets band capped at a count reads as that many or fewer — under 11 complaints is \"10 occurrences or fewer\", under 1 is \"0 occurrences\" — instead of \"Under 11\". A standard whose count is meant to be high, or one with a stated target, is unchanged.",
      ],
    },
  ],
};

export default entry;
