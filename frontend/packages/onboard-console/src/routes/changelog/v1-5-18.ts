import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.18",
  date: "2026-08-11",
  sections: [
    {
      heading: "Changed",
      items: [
        "The operating-period lifecycle now distinguishes the primary next action, revision work, secondary actions, and the separate suspended state.",
      ],
    },
  ],
};

export default entry;
