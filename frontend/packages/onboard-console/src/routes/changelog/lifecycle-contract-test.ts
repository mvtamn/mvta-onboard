import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.172",
  date: "2026-09-09",
  sections: [
    {
      heading: "Changed",
      items: [
        "The assessment lifecycle is now proved against a real database on every build: the real migrations, a month opened the way the console opens one, occurrences scored by the real engine, and the rules the design promises — a recompute keeps a review when nothing changed and resets it when something did; an approved claim removes its occurrence from the count but not from the record; the escalation streak counts only issued months; new evidence withdraws a shared draft and voids the proof; only one proof can be live; two people generating at once take turns. Nothing a person sees changes.",
      ],
    },
  ],
};

export default entry;
