import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.164",
  date: "2026-09-09",
  sections: [
    {
      heading: "Added",
      items: [
        "Every act on an assessment month is now on the record: computing it, each recommendation (with what it replaced), sharing the Validation Draft, finalizing, generating a draft, filing and deciding a dispute — alongside the proof, issue, reopen, exception, and evidence rows that already existed. The row shape is the same whichever step wrote it.",
        "A History tab on Performance Assessment shows that trail for the selected month, newest first, with the recorded detail as written — the record a dispute is answered from.",
      ],
    },
  ],
};

export default entry;
