import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.185",
  date: "2026-09-11",
  sections: [
    {
      heading: "Added",
      items: [
        "Internal groundwork: OnBoard can now read the approved SOP library in SharePoint, and record which folder a Procedure's documents come from. Nothing changes on screen yet — the folder picker that replaces typing SharePoint identifiers by hand comes next.",
      ],
    },
  ],
};

export default entry;
