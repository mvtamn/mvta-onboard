import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.16",
  date: "2026-08-11",
  sections: [
    {
      heading: "Changed",
      items: [
        "Event Planning adds confirmation and clearer feedback for lifecycle actions, fixes dark-mode status contrast, and makes the console shell usable on mobile and tablet.",
      ],
    },
  ],
};

export default entry;
