import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.186",
  date: "2026-09-11",
  sections: [
    {
      heading: "Changed",
      items: [
        "Creating a Decision Matrix Draft: you now browse the approved SharePoint library and pick the SOP, instead of typing its site, drive and item identifiers, version, file name, type and link into seven separate boxes. A file SharePoint has not fully described cannot be picked, and says what is missing rather than being saved as a reference that fails its first document check.",
      ],
    },
  ],
};

export default entry;
