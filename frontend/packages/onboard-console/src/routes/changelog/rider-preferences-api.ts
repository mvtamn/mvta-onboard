import type { ChangelogEntry } from "../changelogEntry.js";

const entry: ChangelogEntry = {
  version: "1.5.200",
  date: "2026-09-12",
  sections: [
    {
      heading: "Added",
      items: [
        "A rider can read and change their own subscription — categories, routes, zones, which channels, and unsubscribing. The API only; the page that uses it is next.",
      ],
    },
  ],
};

export default entry;
